{% extends "base.html.twig" %}

{% block content %}
{{ include('index/tool/navbar.html.twig') }}
    <div class="Formbody">
        <div class="row">
            <div class="col-md-3"></div>
            {{ include('index/tool/carousel.html.twig') }}
        </div>  
        <div class="row">
        <div class="col-md-3"></div>
            <div class="col-md-6 text-center">    
                <button type="button" class="col-md-4 btn btn-outline-danger mainButton2"><u>Déconnection</u></button>
            </div>    
        </div>
        <hr class="my-4">
        {% for subcat in subcats %}
            <div class="row">
                    <div class="col-md-2"></div>
                        <div class="col-md-8">
                            <table class="table">
                                <thead class="thead">
                                    <tr>
                                        <th><u>{{ subcat.name }}</u></th>
                                        <th>Dernier messsage</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {% for topic in subcat.topics %}
                                        <tr>
                                            <td>{{ topic.name }}</td>
                                            <td>@mdo</td>
                                        </tr>
                                    {% endfor %}
                                </tbody>
                            </table>  
                        </div>
                    <div class="col-md-2"></div>
                </div>
                <hr class="my-4">
        {% endfor %}
        {{ include('index/tool/footer.html.twig') }}
    </div>  

{% endblock %}
